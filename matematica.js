const sumar = (num1, num2) => num1 + num2
const multiplicar = (num1, num2) => num1 * num2

function sacarAreaCirculo(radio){

    let PI=Math.PI

    return PI*(radio*radio)



}

const dividir = (num1, num2) => num1 / num2

export default{
    sumar,multiplicar,sacarAreaCirculo,dividir
}